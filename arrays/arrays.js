/*
Se puede acceder a los elementos de un array a través de su índice y con length conocer su longitud.
*/

var miArray = ["uno", "dos", "tres"];
miArray[1];//devuelve "dos"
miArray.length;//devuelve 3; si accedemos a una posición no existente en el array, devolverá undefined.